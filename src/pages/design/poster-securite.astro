---
import BaseHead from "../../components/BaseHead.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../../consts";
import { Image } from "astro:assets";
import ImageGallery from "../../components/ImageGallery.astro";

// Import poster image
import posterImg from "../../assets/portfolio_crea/Affiche_secu_r.png";
---

<!doctype html>
<html lang="fr">
    <head>
        <BaseHead
            title={`Affiche Sécurité Routière | ${SITE_TITLE}`}
            description="Réalisation d'une affiche de prévention pour la sécurité routière."
        />
    </head>
    <body>
        <!-- BACKGROUND -->
        <div class="fixed-background">
            <div class="blob purple"></div>
            <div class="blob blue"></div>
        </div>

        <Header />

        <main class="container project-detail">
            <!-- BREADCRUMB -->
            <a href="/design" class="back-link">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="20"
                    height="20"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><line x1="19" y1="12" x2="5" y2="12"></line><polyline
                        points="12 19 5 12 12 5"></polyline></svg
                >
                Retour aux projets
            </a>

            <!-- HERO PROJET -->
            <section class="project-hero">
                <div class="hero-info">
                    <span class="badge">PHOTOSHOP / COMMUNICATION</span>
                    <h1>Affiche Sécurité Routière</h1>
                    <p class="project-date">Projet de Prévention</p>
                    <div class="tags">
                        <span class="tag">Adobe Photoshop</span>
                        <span class="tag">Retouche Photo</span>
                    </div>
                </div>
            </section>

            <!-- CONTENT GRID -->
            <div class="content-grid">
                <!-- LEFT: TEXT -->
                <article class="project-text glass-panel">
                    <h2>Projet sécurité routière</h2>
                    <p>
                        Ce projet consistait à réaliser une affiche de
                        prévention pour la <strong>sécurité routière</strong>,
                        sur le thème crucial de l'alcool au volant.
                    </p>
                    <p>
                        L'objectif était de créer un visuel impactant et clair,
                        capable d'interpeller les conducteurs et de faire passer
                        un message fort. J'ai réalisé l'intégralité de la
                        composition, de la recherche d'images à la mise en page
                        finale sous <strong>Photoshop</strong>.
                    </p>
                    <p>
                        Vous pouvez visualiser l'affiche en grand format
                        ci-dessous.
                    </p>
                </article>

                <!-- RIGHT: SIDEBAR INFO -->
                <aside class="project-sidebar glass-panel">
                    <h3>Détails du projet</h3>
                    <ul>
                        <li>
                            <span class="label">Contexte</span>
                            <span class="value">Audiovisuel - S1</span>
                        </li>
                        <li>
                            <span class="label">Date</span>
                            <span class="value">2024 - 2025</span>
                        </li>
                        <li>
                            <span class="label">Outils</span>
                            <span class="value">Adobe Photoshop</span>
                        </li>
                        <li>
                            <span class="label">Compétences</span>
                            <span class="value"
                                >Composition, Message, Design</span
                            >
                        </li>
                    </ul>
                </aside>
            </div>

            <!-- POSTER DISPLAY -->
            <section class="project-gallery">
                <h2>L'Affiche Finale</h2>
                <div class="gallery-container">
                    <ImageGallery images={[posterImg]} />
                </div>
            </section>
        </main>

        <Footer />
    </body>
</html>

<style>
    .project-detail {
        padding-top: 2rem;
        padding-bottom: 4rem;
    }

    /* BREADCRUMB */
    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--color-text-light);
        margin-bottom: 2rem;
        font-weight: 500;
        transition: color 0.2s;
    }
    .back-link:hover {
        color: var(--color-primary);
    }

    /* HERO */
    .project-hero {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        margin-bottom: 4rem;
    }
    .hero-image {
        width: 100%;
        height: auto; /* Laisse l'image prendre sa hauteur naturelle */
        padding: 0;
        overflow: visible;
        border-radius: var(--radius-lg);
    }
    .cover-img {
        width: 100%;
        height: auto;
        display: block;
        max-height: 80vh; /* Sécurité pour pas que ça soit immense */
        object-fit: contain; /* Assure qu'on voit tout */
    }

    .hero-info .badge {
        background: rgba(79, 70, 229, 0.1);
        color: var(--color-primary);
        padding: 0.5rem 1rem;
        border-radius: 50px;
        font-weight: 700;
        font-size: 0.8rem;
        display: inline-block;
        margin-bottom: 1rem;
    }
    .hero-info h1 {
        font-size: 3rem;
        margin: 0.5rem 0;
        line-height: 1.1;
    }
    .project-date {
        color: var(--color-text-light);
        font-size: 1.1rem;
        margin-bottom: 1.5rem;
    }

    .tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
    }
    .tag {
        background: rgba(255, 255, 255, 0.5);
        border: 1px solid rgba(255, 255, 255, 0.5);
        padding: 0.3rem 0.8rem;
        border-radius: 6px;
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--color-text-main);
    }

    /* CONTENT GRID */
    .content-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 2rem;
        margin-bottom: 4rem;
    }

    .project-text {
        padding: 2.5rem;
        color: var(--color-text-main);
        line-height: 1.8;
    }
    .project-text h2 {
        font-size: 1.8rem;
        margin-bottom: 1.5rem;
    }
    .project-text h3 {
        font-size: 1.4rem;
        margin: 2rem 0 1rem;
        color: var(--color-primary-dark);
    }
    .project-text p {
        margin-bottom: 1.5rem;
    }

    /* SIDEBAR */
    .project-sidebar {
        padding: 2rem;
        height: fit-content;
    }
    .project-sidebar h3 {
        font-size: 1.2rem;
        margin-bottom: 1.5rem;
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        padding-bottom: 0.5rem;
    }
    .project-sidebar ul {
        list-style: none;
        padding: 0;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }
    .project-sidebar li {
        display: flex;
        flex-direction: column;
        gap: 0.3rem;
    }
    .project-sidebar .label {
        font-size: 0.85rem;
        text-transform: uppercase;
        color: var(--color-text-soft);
        font-weight: 700;
    }
    .project-sidebar .value {
        font-size: 1rem;
        font-weight: 500;
    }

    /* RESPONSIVE */
    @media (max-width: 900px) {
        .content-grid {
            grid-template-columns: 1fr;
        }
        .hero-image {
            height: 300px;
        }
    }
</style>
