---
import BaseHead from "../../components/BaseHead.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../../consts";
import ImageGallery from "../../components/ImageGallery.astro";

// Import images
// Import images
const pages = Object.entries(
    import.meta.glob("../../assets/portfolio_com/communique-froide/*.jpg", {
        eager: true,
    }),
)
    .sort(([pathA], [pathB]) =>
        pathA.localeCompare(pathB, undefined, { numeric: true }),
    )
    .map(([, file]: [string, any]) => file.default);
---

<!doctype html>
<html lang="fr">
    <head>
        <BaseHead
            title={`Communiqué de Presse | ${SITE_TITLE}`}
            description="Rédaction d'un communiqué de presse pour l'entreprise ColdAll."
        />
    </head>
    <body class="project-page">
        <!-- BACKGROUND -->
        <div class="fixed-background">
            <div class="blob purple"></div>
            <div class="blob blue"></div>
        </div>

        <Header />

        <main class="container project-detail">
            <!-- BREADCRUMB -->
            <a href="/communication" class="back-link">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="20"
                    height="20"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><line x1="19" y1="12" x2="5" y2="12"></line><polyline
                        points="12 19 5 12 12 5"></polyline></svg
                >
                Retour aux projets
            </a>

            <!-- HERO PROJET -->
            <section class="project-hero">
                <div class="hero-info">
                    <span class="badge">RELATIONS PRESSE / RÉDACTION</span>
                    <h1>Communiqué : ColdAll</h1>
                    <p class="project-date">
                        Projet Relations Presse - SAE 102
                    </p>
                    <div class="tags">
                        <span class="tag">Communiqué de presse</span>
                        <span class="tag">Dossier de presse</span>
                        <span class="tag">Innovation</span>
                    </div>
                </div>
            </section>

            <!-- CONTENT GRID -->
            <div class="content-grid">
                <!-- LEFT: TEXT -->
                <article class="project-text glass-panel">
                    <h2>🗞️ Lancement d'un réfrigérateur intelligent</h2>
                    <p>
                        Dans le cadre de ce projet de Relations Presse, j'ai eu
                        pour mission de concevoir les supports de communication
                        pour le lancement d'un produit innovant : le
                        réfrigérateur connecté de l'entreprise <strong
                            >ColdAll</strong
                        >.
                    </p>
                    <p>
                        Ce réfrigérateur se distingue par l'intégration d'écrans
                        tactiles permettant un contrôle total : gestion des
                        stocks, recettes intelligentes, et contrôle de la
                        température par zone.
                    </p>
                    <p>
                        <strong>Ma mission :</strong> Rédiger un communmiqué de presse
                        percutant et un dossier de presse complet destinés aux journalistes
                        spécialisés Tech et Maison, afin de maximiser la couverture
                        médiatique du lancement.
                    </p>
                </article>

                <!-- RIGHT: SIDEBAR INFO -->
                <aside class="project-sidebar glass-panel">
                    <h3>Détails du projet</h3>
                    <ul>
                        <li>
                            <span class="label">Contexte</span>
                            <span class="value">SAE 102 - Relations Presse</span
                            >
                        </li>
                        <li>
                            <span class="label">Client Fictif</span>
                            <span class="value">ColdAll</span>
                        </li>
                        <li>
                            <span class="label">Date</span>
                            <span class="value">2024</span>
                        </li>
                    </ul>
                </aside>
            </div>

            <!-- GALLERY -->
            <section class="project-gallery">
                <h2>Aperçu des supports</h2>
                <div class="gallery-container">
                    <ImageGallery images={pages as any[]} />
                </div>
            </section>
        </main>

        <Footer />
    </body>
</html>

<style>
    .project-detail {
        padding-top: 2rem;
        padding-bottom: 4rem;
    }
    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--color-text-light);
        margin-bottom: 2rem;
        font-weight: 500;
        transition: color 0.2s;
    }
    .back-link:hover {
        color: var(--color-primary);
    }
    .project-hero {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        margin-bottom: 4rem;
    }
    .hero-info .badge {
        background: rgba(79, 70, 229, 0.1);
        color: var(--color-primary);
        padding: 0.5rem 1rem;
        border-radius: 50px;
        font-weight: 700;
        font-size: 0.8rem;
        display: inline-block;
        margin-bottom: 1rem;
    }
    .hero-info h1 {
        font-size: 3rem;
        margin: 0.5rem 0;
        line-height: 1.1;
    }
    .project-date {
        color: var(--color-text-light);
        font-size: 1.1rem;
        margin-bottom: 1.5rem;
    }
    .tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
    }
    .tag {
        background: rgba(255, 255, 255, 0.5);
        border: 1px solid rgba(255, 255, 255, 0.5);
        padding: 0.3rem 0.8rem;
        border-radius: 6px;
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--color-text-main);
    }
    .content-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 2rem;
        margin-bottom: 4rem;
    }
    .project-text {
        padding: 2.5rem;
        color: var(--color-text-main);
        line-height: 1.8;
    }
    .project-text h2 {
        font-size: 1.8rem;
        margin-bottom: 1.5rem;
    }
    .project-text p {
        margin-bottom: 1.5rem;
    }
    .project-sidebar {
        padding: 2rem;
        height: fit-content;
    }
    .project-sidebar h3 {
        font-size: 1.2rem;
        margin-bottom: 1.5rem;
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        padding-bottom: 0.5rem;
    }
    .project-sidebar ul {
        list-style: none;
        padding: 0;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }
    .project-sidebar li {
        display: flex;
        flex-direction: column;
        gap: 0.3rem;
    }
    .project-sidebar .label {
        font-size: 0.85rem;
        text-transform: uppercase;
        color: var(--color-text-soft);
        font-weight: 700;
    }
    .project-sidebar .value {
        font-size: 1rem;
        font-weight: 500;
    }
    .project-gallery h2 {
        font-size: 2rem;
        margin-bottom: 2rem;
        text-align: center;
    }
    .gallery-grid {
        display: grid;
        gap: 1.5rem;
    }
    .gallery-grid.single-image {
        grid-template-columns: 1fr;
        max-width: 800px;
        margin: 0 auto;
    }
    .gallery-item {
        padding: 0;
        overflow: hidden;
        transition: transform 0.3s ease;
        cursor: pointer;
    }
    .gallery-item:hover {
        transform: scale(1.02);
    }
    .gallery-item img {
        width: 100%;
        height: auto;
        display: block;
    }
    .lightbox {
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.9);
        z-index: 1000;
        display: flex;
        justify-content: center;
        align-items: center;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.3s ease;
    }
    .lightbox.active {
        opacity: 1;
        pointer-events: all;
    }
    .lightbox-content {
        max-width: 90%;
        max-height: 90vh;
        position: relative;
    }
    .lightbox-content img {
        max-width: 100%;
        max-height: 90vh;
        border-radius: 8px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
    }
    .close-btn {
        position: absolute;
        top: 20px;
        right: 20px;
        background: none;
        border: none;
        color: white;
        font-size: 3rem;
        cursor: pointer;
        z-index: 1001;
        transition: transform 0.2s;
    }
    .close-btn:hover {
        transform: scale(1.1);
        color: var(--color-primary);
    }
    @media (max-width: 900px) {
        .content-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
