---
import BaseHead from "../../components/BaseHead.astro";
import Header from "../../components/Header.astro";
import Footer from "../../components/Footer.astro";
import { SITE_TITLE, SITE_DESCRIPTION } from "../../consts";
import { Image } from "astro:assets";

// Import images
import auditImg from "../../assets/portfolio_com/audit-temu.jpg";

const galleryImages = [auditImg];
---

<!doctype html>
<html lang="fr">
    <head>
        <BaseHead
            title={`Audit TEMU | ${SITE_TITLE}`}
            description="Audit éco-gestion et stratégie web de TEMU."
        />
    </head>
    <body class="project-page">
        <!-- BACKGROUND -->
        <div class="fixed-background">
            <div class="blob purple"></div>
            <div class="blob blue"></div>
        </div>

        <Header />

        <main class="container project-detail">
            <!-- BREADCRUMB -->
            <a href="/communication" class="back-link">
                <svg
                    xmlns="http://www.w3.org/2000/svg"
                    width="20"
                    height="20"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    ><line x1="19" y1="12" x2="5" y2="12"></line><polyline
                        points="12 19 5 12 12 5"></polyline></svg
                >
                Retour aux projets
            </a>

            <!-- HERO PROJET -->
            <section class="project-hero">
                <div class="hero-info">
                    <span class="badge">MARKETING / AUDIT</span>
                    <h1>Audit Eco-Gestion TEMU</h1>
                    <p class="project-date">Projet Universitaire - SAE 101</p>
                    <div class="tags">
                        <span class="tag">Analyse SWOT</span>
                        <span class="tag">Stratégie Web</span>
                        <span class="tag">Eco-Conception</span>
                    </div>
                </div>
            </section>

            <!-- CONTENT GRID -->
            <div class="content-grid">
                <!-- LEFT: TEXT -->
                <article class="project-text glass-panel">
                    <h2>📊 Analyse stratégique d'un géant du e-commerce</h2>
                    <p>
                        Dans le cadre de l'enseignement d'Eco-Gestion, nous
                        avons réalisé un audit complet de la plateforme <strong
                            >TEMU</strong
                        >. L'objectif était de décrypter la stratégie agressive
                        de ce nouvel acteur du e-commerce et d'analyser son
                        impact économique et écologique.
                    </p>
                    <p>
                        Notre dossier d'analyse se penche sur plusieurs axes :
                    </p>
                    <ul>
                        <li>
                            <strong>Le modèle économique :</strong> Comment TEMU parvient-il
                            à casser les prix ?
                        </li>
                        <li>
                            <strong>La stratégie marketing :</strong> Analyse de leur
                            présence massive sur les réseaux sociaux et de la gamification
                            de l'achat.
                        </li>
                        <li>
                            <strong>L'impact environnemental :</strong> Critique de
                            la fast-fashion et de la logistique mondiale.
                        </li>
                    </ul>
                    <p>
                        Ce travail a nécessité une recherche documentaire
                        approfondie et une capacité de synthèse pour mettre en
                        lumière les forces et les faiblesses (SWOT) de
                        l'entreprise.
                    </p>
                </article>

                <!-- RIGHT: SIDEBAR INFO -->
                <aside class="project-sidebar glass-panel">
                    <h3>Détails du projet</h3>
                    <ul>
                        <li>
                            <span class="label">Contexte</span>
                            <span class="value">SAE 101 - Eco-Gestion</span>
                        </li>
                        <li>
                            <span class="label">Date</span>
                            <span class="value">2024</span>
                        </li>
                        <li>
                            <span class="label">Livrable</span>
                            <span class="value">Dossier d'analyse (PDF)</span>
                        </li>
                    </ul>
                </aside>
            </div>

            <!-- GALLERY -->
            <section class="project-gallery">
                <h2>Aperçu du dossier</h2>
                <div class="gallery-grid single-image">
                    {
                        galleryImages.map((img, index) => (
                            <div
                                class="gallery-item glass-panel"
                                data-index={index}
                            >
                                <Image
                                    src={img}
                                    alt={`Audit Eco-Gestion TEMU`}
                                />
                            </div>
                        ))
                    }
                </div>
            </section>
        </main>

        <!-- LIGHTBOX OVERLAY -->
        <div id="lightbox" class="lightbox">
            <button class="close-btn">&times;</button>
            <div class="lightbox-content">
                <img id="lightbox-img" src="" alt="Agrandissement" />
            </div>
        </div>

        <Footer />

        <script>
            // LOGIQUE LIGHTBOX
            const lightbox = document.getElementById("lightbox");
            const lightboxImg = document.getElementById("lightbox-img");
            const closeBtn = document.querySelector(".close-btn");

            if (
                lightbox &&
                lightboxImg instanceof HTMLImageElement &&
                closeBtn
            ) {
                const items = document.querySelectorAll(".gallery-item img");

                items.forEach((img) => {
                    if (img.parentElement) {
                        img.parentElement.addEventListener("click", () => {
                            if (img instanceof HTMLImageElement) {
                                lightboxImg.src = img.src;
                                lightbox.classList.add("active");
                                document.body.style.overflow = "hidden";
                            }
                        });
                    }
                });

                const closeLightbox = () => {
                    lightbox.classList.remove("active");
                    document.body.style.overflow = "auto";
                };

                closeBtn.addEventListener("click", closeLightbox);
                lightbox.addEventListener("click", (e) => {
                    if (e.target === lightbox) closeLightbox();
                });

                document.addEventListener("keydown", (e) => {
                    if (e.key === "Escape") closeLightbox();
                });
            }
        </script>
    </body>
</html>

<style>
    .project-detail {
        padding-top: 2rem;
        padding-bottom: 4rem;
    }
    .back-link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        color: var(--color-text-light);
        margin-bottom: 2rem;
        font-weight: 500;
        transition: color 0.2s;
    }
    .back-link:hover {
        color: var(--color-primary);
    }
    .project-hero {
        display: flex;
        flex-direction: column;
        gap: 2rem;
        margin-bottom: 4rem;
    }
    .hero-info .badge {
        background: rgba(79, 70, 229, 0.1);
        color: var(--color-primary);
        padding: 0.5rem 1rem;
        border-radius: 50px;
        font-weight: 700;
        font-size: 0.8rem;
        display: inline-block;
        margin-bottom: 1rem;
    }
    .hero-info h1 {
        font-size: 3rem;
        margin: 0.5rem 0;
        line-height: 1.1;
    }
    .project-date {
        color: var(--color-text-light);
        font-size: 1.1rem;
        margin-bottom: 1.5rem;
    }
    .tags {
        display: flex;
        flex-wrap: wrap;
        gap: 0.5rem;
    }
    .tag {
        background: rgba(255, 255, 255, 0.5);
        border: 1px solid rgba(255, 255, 255, 0.5);
        padding: 0.3rem 0.8rem;
        border-radius: 6px;
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--color-text-main);
    }
    .content-grid {
        display: grid;
        grid-template-columns: 2fr 1fr;
        gap: 2rem;
        margin-bottom: 4rem;
    }
    .project-text {
        padding: 2.5rem;
        color: var(--color-text-main);
        line-height: 1.8;
    }
    .project-text h2 {
        font-size: 1.8rem;
        margin-bottom: 1.5rem;
    }
    .project-text ul {
        margin-bottom: 1.5rem;
        padding-left: 1.5rem;
    }
    .project-text li {
        margin-bottom: 0.5rem;
    }
    .project-text p {
        margin-bottom: 1.5rem;
    }
    .project-sidebar {
        padding: 2rem;
        height: fit-content;
    }
    .project-sidebar h3 {
        font-size: 1.2rem;
        margin-bottom: 1.5rem;
        border-bottom: 1px solid rgba(0, 0, 0, 0.1);
        padding-bottom: 0.5rem;
    }
    .project-sidebar ul {
        list-style: none;
        padding: 0;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }
    .project-sidebar li {
        display: flex;
        flex-direction: column;
        gap: 0.3rem;
    }
    .project-sidebar .label {
        font-size: 0.85rem;
        text-transform: uppercase;
        color: var(--color-text-soft);
        font-weight: 700;
    }
    .project-sidebar .value {
        font-size: 1rem;
        font-weight: 500;
    }
    .project-gallery h2 {
        font-size: 2rem;
        margin-bottom: 2rem;
        text-align: center;
    }
    .gallery-grid {
        display: grid;
        gap: 1.5rem;
    }
    .gallery-grid.single-image {
        grid-template-columns: 1fr;
        max-width: 800px;
        margin: 0 auto;
    }
    .gallery-item {
        padding: 0;
        overflow: hidden;
        transition: transform 0.3s ease;
        cursor: pointer;
    }
    .gallery-item:hover {
        transform: scale(1.02);
    }
    .gallery-item img {
        width: 100%;
        height: auto;
        display: block;
    }
    .lightbox {
        position: fixed;
        inset: 0;
        background: rgba(0, 0, 0, 0.9);
        z-index: 1000;
        display: flex;
        justify-content: center;
        align-items: center;
        opacity: 0;
        pointer-events: none;
        transition: opacity 0.3s ease;
    }
    .lightbox.active {
        opacity: 1;
        pointer-events: all;
    }
    .lightbox-content {
        max-width: 90%;
        max-height: 90vh;
        position: relative;
    }
    .lightbox-content img {
        max-width: 100%;
        max-height: 90vh;
        border-radius: 8px;
        box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
    }
    .close-btn {
        position: absolute;
        top: 20px;
        right: 20px;
        background: none;
        border: none;
        color: white;
        font-size: 3rem;
        cursor: pointer;
        z-index: 1001;
        transition: transform 0.2s;
    }
    .close-btn:hover {
        transform: scale(1.1);
        color: var(--color-primary);
    }
    @media (max-width: 900px) {
        .content-grid {
            grid-template-columns: 1fr;
        }
    }
</style>
