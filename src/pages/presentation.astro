---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="fr">
	<head>
		<BaseHead title={`Présentation | ${SITE_TITLE}`} description={SITE_DESCRIPTION} />
		<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;700;900&display=swap" rel="stylesheet">
	</head>
	<body>
		<Header />
		<main>
			<section class="hero-background small-hero">
				<div class="overlay"></div> 
				<div class="hero-content">
					<h1>Présentation</h1>
				</div>
			</section>

			<section class="presentation-container">
				
				<div class="content-box">
					<h2>Mon Parcours</h2>
					<p>
						Je m’appelle Tom Marcheron et je suis actuellement étudiant en Bachelor Universitaire de Technologie des Métiers
						du Multimédia et de l’Internet (MMI). Grâce à cette formation, je peux développer des compétences dans la communication,
						le design, le développement web et l'audiovisuel. J’aime bien la création et la technologie, c’est pourquoi j’ai
						choisi d’intégrer le BUT MMI.
						<br><br>
						Étant passionné depuis tout petit par le mixage sonore et l’éclairage DMX, j’ai une bonne base sur l’utilisation de
						matériels semi-professionnels, notamment un contrôleur DJ de la marque Pioneer, et des projecteurs 
						(lyres, par LEDs, barres LED…) contrôlables en DMX grâce à une interface SunLite me permettant de créer des ambiances
						immersives pour certains événements privés que je peux faire.<br>
						Étant donné que c’est une passion, j'aimerais me diriger vers ce métier pour en faire ma passion. Je suis à la recherche
						d’opportunités pour intégrer ce milieu professionnel et développer mes compétences dans ce domaine.
					</p>
				</div>

				<div class="id-card-wrapper">
					<div class="id-card">
						<div class="id-photo">
							<div class="placeholder-icon">TM</div>
						</div>
						<div class="id-details">
							<h3>Tom Marcheron</h3>
							<span class="id-job">Étudiant MMI</span>
							
							<ul class="id-list">
								<li>
									<span class="icon">🎂</span>
									<div class="info">
										<span class="label">Âge</span>
										<span class="value">19 ans</span> </div>
								</li>
								<li>
									<span class="icon">📍</span>
									<div class="info">
										<span class="label">Localisation</span>
										<span class="value">Saint-Lô, France</span>
									</div>
								</li>
								<li>
									<span class="icon">🚗</span>
									<div class="info">
										<span class="label">Mobilité</span>
										<span class="value">Permis B (Véhiculé)</span>
									</div>
								</li>
								<li>
									<span class="icon">🎓</span>
									<div class="info">
										<span class="label">Formation</span>
										<span class="value">BUT Métiers du Multimédia</span>
									</div>
								</li>
							</ul>
						</div>
					</div>
				</div>

			</section>
		</main>
		<Footer />
	</body>
</html>

<style>
	/* --- VARIABLES & GLOBAL --- */
	:global(body) {
		font-family: 'Outfit', sans-serif !important;
		margin: 0;
		background-color: #f9f9f9;
		color: #333;
	}
	a { text-decoration: none; }
	main { width: 100%; padding: 0 0 4rem 0; }

	/* --- HERO SECTION (RÉDUITE) --- */
	.hero-background {
		position: relative;
		/* Moins haut que la page d'accueil */
		min-height: 40vh; 
		display: flex;
		align-items: center;
		justify-content: center;
		text-align: center;
		color: white;
		background-image: url('https://images.unsplash.com/photo-1550751827-4bd374c3f58b?auto=format&fit=crop&q=80');
		background-size: cover;
		background-position: center;
	}

	.overlay {
		position: absolute; inset: 0;
		background: rgba(0, 0, 0, 0.6); z-index: 1;
	}

	.hero-content { position: relative; z-index: 2; padding: 2rem; }

	h1 { font-size: 3.5rem; font-weight: 800; margin: 0; }
	.text-gradient { color: #8B5CF6; } /* Violet punchy */

	/* --- CONTENEUR --- */
	.presentation-container {
		max-width: 900px;
		margin: -50px auto 0 auto;
		position: relative; z-index: 10;
		padding: 0 1.5rem;
		display: flex;
		flex-direction: column;
		gap: 2rem; /* Espace entre le texte et la carte ID */
	}

	/* --- BOITE DE TEXTE --- */
	.content-box {
		background: white;
		padding: 3rem;
		border-radius: 20px;
		box-shadow: 0 10px 30px rgba(0,0,0,0.05);
	}

	.content-box h2 {
		font-size: 2rem; margin-top: 0; margin-bottom: 1.5rem;
		color: #22D3EE; /* Cyan */
	}

	.content-box p {
		font-size: 1.1rem; color: #555; line-height: 1.8;
	}

	/* --- CARTE D'IDENTITÉ --- */
	.id-card-wrapper {
		display: flex;
		justify-content: center;
	}

	.id-card {
		background: white;
		width: 100%;
		max-width: 700px;
		border-radius: 20px;
		box-shadow: 0 10px 30px rgba(0,0,0,0.08);
		display: flex;
		overflow: hidden;
		border: 1px solid #eee;
	}

	/* Partie Gauche (Photo) */
	.id-photo {
		background: linear-gradient(135deg, #0E0078, #22D3EE);
		width: 30%;
		display: flex;
		align-items: center;
		justify-content: center;
		padding: 2rem;
	}

	.placeholder-icon {
		width: 100px; height: 100px;
		background: white;
		border-radius: 50%;
		display: flex; align-items: center; justify-content: center;
		font-size: 2rem; font-weight: 900; color: #0E0078;
		box-shadow: 0 5px 15px rgba(0,0,0,0.2);
	}

	/* Partie Droite (Infos) */
	.id-details {
		padding: 2rem;
		flex: 1;
	}

	.id-details h3 {
		margin: 0; font-size: 1.8rem; color: #111;
	}

	.id-job {
		display: inline-block;
		background: #f0fdf4; color: #16a34a; /* Vert style "Actif" */
		padding: 4px 12px; border-radius: 50px;
		font-size: 0.8rem; font-weight: 700;
		margin-bottom: 1.5rem; margin-top: 0.5rem;
	}

	.id-list {
		list-style: none; padding: 0; margin: 0;
		display: grid;
		grid-template-columns: 1fr 1fr; /* 2 colonnes */
		gap: 1.5rem;
	}

	.id-list li {
		display: flex; align-items: center; gap: 1rem;
	}

	.icon { font-size: 1.8rem; }

	.info { display: flex; flex-direction: column; }
	.label { font-size: 0.75rem; color: #888; text-transform: uppercase; font-weight: 700; }
	.value { font-size: 1.1rem; color: #333; font-weight: 600; }

	/* --- RESPONSIVE MOBILE --- */
	@media (max-width: 768px) {
		.id-card { flex-direction: column; }
		.id-photo { width: 100%; padding: 1.5rem; }
		.id-list { grid-template-columns: 1fr; } /* 1 colonne sur mobile */
		.presentation-container { margin-top: -2rem; }
	}
</style>